## 1. 后端授权流程

- [x] 1.1 抽取 OAuth 回调 URL 解析与授权码换 token 的共享 helper，供现有 OAuth 助手和账号重新授权接口复用
- [x] 1.2 新增账号授权更新 helper，只更新 `client_id`、加密后的 `refresh_token`、`refresh_token_updated_at` 和必要的刷新状态字段
- [x] 1.3 新增 `POST /api/accounts/<account_id>/reauthorize` 接口，校验登录、账号存在、账号类型为 Outlook，并拒绝 IMAP 账号
- [x] 1.4 在重新授权接口中保存新授权信息后清理旧刷新失败错误，并调用现有单账号刷新逻辑执行自动验证
- [x] 1.5 让重新授权接口返回授权更新结果和自动刷新结果，失败响应使用现有错误结构与敏感信息脱敏规则

## 2. 前端重新授权入口

- [x] 2.1 扩展现有 OAuth 弹窗支持“更新已有账号”模式，预填账号邮箱并隐藏新账号保存专用字段
- [x] 2.2 在 Outlook 账号编辑弹窗中增加重新授权入口，IMAP 账号不显示该入口
- [x] 2.3 在刷新失败详情中增加重新授权入口，自动带入失败账号上下文
- [x] 2.4 实现前端提交回调 URL 到账号重新授权接口，并展示授权更新、自动刷新成功、自动刷新失败三类结果
- [x] 2.5 重新授权完成后刷新账号列表、刷新管理列表和当前账号状态缓存，确保旧失败提示不会残留

## 3. 文档与兼容

- [x] 3.1 更新 `docs/api.md`，记录账号重新授权接口、请求体、响应字段和错误行为
- [x] 3.2 确认现有 `/api/oauth/exchange-token`、账号导入、账号编辑、单账号刷新行为保持兼容
- [x] 3.3 确认无需数据库迁移，复用 `refresh_token_updated_at` 和现有刷新状态字段

## 4. 测试验证

- [x] 4.1 增加后端测试：重新授权成功只更新授权字段，并保留邮箱、密码、分组、状态、转发、代理、备注、别名和标签
- [x] 4.2 增加后端测试：IMAP 账号和不存在账号的重新授权请求不会修改数据
- [x] 4.3 增加后端测试：旧失败状态在重新授权后被清理，自动刷新成功时最终状态为 `success`
- [x] 4.4 增加后端测试：自动刷新失败时最终状态为 `failed`，并保存新的错误信息
- [x] 4.5 增加前端或静态测试：Outlook 账号显示重新授权入口，IMAP 账号不显示，新模式不会走保存新账号流程
- [x] 4.6 运行相关测试：OAuth token 预览、刷新状态、账号编辑/敏感字段保护相关测试，并记录结果
